Skills live under `skills/`. Every skill has its own folder with a `SKILL.md`.

Every skill must have a reference entry in `skills/README.md` and in `.claude-plugin/plugin.json`.

Each entry in `skills/README.md` must link the skill name to its `SKILL.md`.
